#include <stdio.h>
#include "dog.h"

/*
 * print_dog - print dog struct
 *
 * @d:dog struct
 *
 * Return:nothing
 */
void print_dog(struct dog *d)
{

	if ((*d).ege && (*d).owner == '\0')
		printf(nil);
	if ((*d).name == '\0')
		printf(name":"(nil));
	if (d == '\0')
		printf(nothing)
		
}
