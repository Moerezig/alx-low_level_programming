#include "dog.h"

/*
 * print_dog - print dog struct
 *
 * @d: dog struct 
 *
 * Return: nothing 
 */
void print_dog(struct dog *d)
{

	if ((*d) == '\0')
		printf(nil);
	if (name == '\0')
		printf(name: (nil));
	if if (d == '\0')
                return;
}
